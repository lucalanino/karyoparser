% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/validate.R
\name{.assess_karyotypes}
\alias{.assess_karyotypes}
\title{Assess karyotype strings: apply all fixes, classify rows}
\usage{
.assess_karyotypes(x)
}
\arguments{
\item{x}{Character vector of raw karyotype strings.}
}
\value{
A named list:
\itemize{
\item \verb{$processed} — fully-fixed character vector (dirty fixed + chimeric truncated)
\item \verb{$reported_issues} — long tibble (row_index, karyotype, issue_type,
issue_detail) combining dirty issues from raw, chimeric from after dirty
fix, and structural from after all fixes.
\item \verb{$unfixable_row_indices} — integer: rows with structural issues that
remain after all fixes.
\item \verb{$dirty_row_indices} — integer: rows that had fixable dirty markers.
\item \verb{$chimeric_row_indices} — integer: rows that had chimeric separator
(detected after dirty fixes, before truncation).
}
}
\description{
Single source of truth for fixable/unfixable classification, shared by
\code{check_karyo()}, \code{preprocess_karyo()}, and the \code{parse_karyo()} guard.
}
\details{
Detection strategy:
\itemize{
\item Dirty issues detected on the RAW string (before fixes), so issue type
columns report what was actually wrong with the input.
\item Structural issues detected on the FULLY-FIXED string (after dirty fixes
and chimeric truncation), so false positives from dirty-marker
contamination (e.g. trailing narrative making \code{no_sex_complement} fire)
are eliminated.
}
}
\keyword{internal}
