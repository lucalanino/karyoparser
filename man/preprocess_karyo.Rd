% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess_karyo.R
\name{preprocess_karyo}
\alias{preprocess_karyo}
\title{Clean and Normalize ISCN Karyotype Strings}
\usage{
preprocess_karyo(
  karyotypes,
  karyotype_column = NULL,
  id_column = NULL,
  on_chimeric = c("default", "host", "donor"),
  verbose = FALSE
)
}
\arguments{
\item{karyotypes}{Character vector of raw karyotype strings, the
\code{karyo_check} tibble returned by \code{check_karyo()}, or a data frame
containing a karyotype
column. When a \code{karyo_check} tibble is supplied, the assessment it already
computed is reused directly -- no re-scanning -- and any id column named
upstream is propagated automatically. When a plain data frame is supplied,
name the karyotype column with \code{karyotype_column}.}

\item{karyotype_column}{Character. Name of the karyotype column.
\strong{Required} when \code{x} is a plain data frame -- columns are never guessed.
Ignored for character vector or \code{karyo_check} input.}

\item{id_column}{Character. Name of the id column. Optional: \code{NULL}
(default) means the data has no identifier, and one is never inferred.
Ignored for character vector or \code{karyo_check} input, where the id is
propagated from the upstream object's attributes.
Duplicate ids are warned about, not rejected: rows are matched on the
karyotype string and never on the id, so parsing is unaffected -- but
downstream joins on the column may fan out.}

\item{on_chimeric}{Character string controlling which clone is kept for
chimeric karyotypes (those containing a \verb{//} separator). One of:
\itemize{
\item \code{"default"}: host clone (before \verb{//}) for normal chimeras; donor clone
(after \verb{//}) for zero-host chimeras (\verb{.//} prefix).
\item \code{"host"}: host clone only; zero-host chimeras become NA (the NA is
policy-induced, so the row stays classified as fixable, not unfixable).
\item \code{"donor"}: everything after \verb{//} (the donor population).
Rows with two or more \verb{//} separators are always unfixable. The choice is
baked into the \code{preprocessed} column and recorded so \code{parse_karyo()} can
reuse it.
}}

\item{verbose}{Logical. If \code{TRUE}, prints a processing summary. Default
\code{FALSE}.}
}
\value{
A \code{karyo_preprocessed} tibble. Columns: optional id column (first,
when given or propagated), \code{original} (raw input, always unchanged),
\code{preprocessed} (fully normalized string, \code{NA_character_} for unfixable
rows), \code{status} (\code{"clean"}, \code{"fixed"}, or \code{"unfixable"}).

The returned tibble can be passed directly to \code{parse_karyo()} without
specifying \code{karyotype_column} or \code{id_column} -- both are carried on the
object's class and cached attributes.
}
\description{
The complete cleaning and normalization pipeline: fixes every fixable
issue type (see \code{check_karyo()}) in a fixed order, then runs
\code{normalize_iscn()} as a final pass. Output is fully normalized and
parser-ready. This is the only place in the workflow where cleaning or
normalization occurs -- \code{parse_karyo()} does none. See
\code{vignette("data-cleaning")} for the exact fix order and worked examples.
}
\details{
\code{zero_host_chimera} strings (\verb{.//} or \verb{//} prefix) are donor-only chimeras
with no host metaphases. The leading \verb{.//} prefix is stripped, leaving the
donor clone(s), which are then parsed (under \code{on_chimeric = "host"} these
rows are instead returned NA). A remaining non-ASCII character with no
known automatic fix (\code{stray_non_ascii}) is detected but not modified; such
rows are returned as \code{NA} (unfixable).
}
\seealso{
\code{\link[=check_karyo]{check_karyo()}}, \code{vignette("data-cleaning")}
}
