% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/check_karyo.R
\name{check_karyo}
\alias{check_karyo}
\title{Check Karyotype Strings for Issues}
\usage{
check_karyo(
  karyotypes,
  karyotype_column = NULL,
  id_column = NULL,
  verbose = FALSE
)
}
\arguments{
\item{karyotypes}{Character vector of karyotype strings, or a data frame
containing a karyotype column. If a data frame, the karyotype column is
auto-detected from common names (\code{karyotype}, \code{iscn}, etc.) or
specified via \code{karyotype_column}. An id column is also auto-detected
or specified via
\code{id_column}; when found it is included as the first column of the output
and propagated through subsequent pipeline steps.}

\item{karyotype_column}{Character. Name of the karyotype column when
\code{karyotypes} is a data frame. If \code{NULL} (default), auto-detected from
common names. Ignored when \code{karyotypes} is a character vector.}

\item{id_column}{Character. Name of the id column when \code{karyotypes} is a
data frame. If \code{NULL} (default), auto-detected from common names
(\code{sample_id}, \code{patient_id}, \code{id}, \code{mrn}, etc.) and used silently. Ignored
when \code{karyotypes} is a character vector.}

\item{verbose}{Logical. If \code{TRUE}, prints a count summary and per-issue-type
breakdown. Default \code{FALSE}.}
}
\value{
A \code{karyo_check} tibble with \code{length(karyotypes)} rows (or
\code{nrow(karyotypes)} when input is a data frame). Columns: optional id column
(first, when detected), \code{karyotype} (full input string), \code{fixable},
\code{unfixable}, then one integer column per unfixable issue type
(alphabetical), then one integer column per fixable issue type
(alphabetical). The tibble can be passed directly to
\code{preprocess_karyo()}, which will reuse the cached assessment and
propagate the id column without re-scanning.
}
\description{
Checks raw karyotype strings for formatting artifacts and structural errors.
Read-only: this is a diagnostic step and never modifies a karyotype string
(cleaning happens in \code{preprocess_karyo()}). Returns a wide-format tibble
with one row per input string: each possible issue type is a column
(\code{0}/\code{1}), plus summary \code{fixable} and \code{unfixable} columns. When \code{verbose = TRUE}, prints a count summary and per-issue-type breakdown.
}
\details{
Fixable issue types (resolvable by \code{parse_karyo()} under \code{on_issues = "preprocess"}): \code{unicode_notation}, \code{non_ascii_homoglyph},
\code{embedded_newline}, \code{html_entities}, \code{leading_dot}, \code{count_sex_separator},
\code{fish_notation}, \code{trailing_narrative}, \code{midstring_linewrap},
\code{missing_sex_comma}, \code{mar_space}, \code{non_clonal_sca}, \code{chimeric_separator},
\code{zero_host_chimera}.

Unfixable issue types (always returned as NA by \code{parse_karyo()}):
\code{multiple_chimeric_separator}, \code{empty}, \code{no_chromosome_count},
\code{updated_iscn}, \code{unbalanced_parentheses}, \code{unbalanced_brackets},
\code{no_sex_complement}, \code{single_token}, \code{constitutional_sex_complement},
\code{mosaic_karyotype}, \code{invalid_idem}, \code{unparseable_bracket},
\code{stray_non_ascii}.

See \code{vignette("data-cleaning")} for what each issue type means, worked
examples, and how to resolve the unfixable ones.
}
\seealso{
\code{\link[=preprocess_karyo]{preprocess_karyo()}}, \code{vignette("data-cleaning")}
}
