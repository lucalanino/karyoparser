% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{flag_unpreprocessed}
\alias{flag_unpreprocessed}
\title{Flag Unpreprocessed ISCN Strings}
\usage{
flag_unpreprocessed(x)
}
\arguments{
\item{x}{Character vector of karyotype strings.}
}
\value{
A tibble with columns: \code{row_index}, \code{karyotype} (truncated to 40
chars), \code{issue_type}, \code{issue_detail}. Each issue generates one row;
multiple issues on the same input row appear as separate rows.
}
\description{
Detects dirty markers in karyotype strings that \code{normalize_iscn()} cannot
fix. Called internally by \code{check_karyo()}.
}
\details{
Detected issue types:
\itemize{
\item \code{html_entities}: contains \verb{&lt;}, \verb{&gt;}, or \verb{&amp;}
\item \code{leading_dotslash}: string starts with \verb{.//} (after trimming)
\item \code{leading_dot}: string starts with dot(s) before a digit
\item \code{fish_notation}: FISH/nuc ish suffix after last clone bracket
\item \code{trailing_narrative}: bracket followed by dot (\verb{] .text}) OR
space-dot-capital pattern (\code{ .Text}, no bracket required)
\item \code{midstring_linewrap}: \verb{, .lowercase} or \verb{, .+} mid-string line-wrap artifact
\item \code{missing_sex_comma}: sex complement followed by space instead of comma
(e.g. \verb{46,XX der(...)} → \verb{46,XX,der(...)})
\item \code{mar_space}: space between count and \code{mar} token (e.g. \verb{+1~4 mar})
}
}
\keyword{internal}
