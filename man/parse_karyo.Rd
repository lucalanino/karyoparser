% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_karyo.R
\name{parse_karyo}
\alias{parse_karyo}
\title{Parse ISCN Karyotype Strings}
\usage{
parse_karyo(
  karyotypes,
  rules = rules_table(),
  karyotype_column = NULL,
  id_column = NULL,
  .return = c("tibble", "data.frame"),
  verbose = TRUE,
  on_issues = c("fix", "warn", "stop")
)
}
\arguments{
\item{karyotypes}{Either a character vector of karyotype strings or a data.frame
containing a karyotype column. If a data.frame, specify the column name via
karyotype_column parameter.}

\item{rules}{A data.frame of parsing rules (default: rules_table()). Must contain
columns: flag_name, regex, category, priority, counts_for_monosomal.
Use custom data.frame for specialized parsing needs.}

\item{karyotype_column}{Character string specifying the column name containing
karyotypes when input is a data.frame. If NULL (default), tries to auto-detect
from common names: "karyotype", "Karyotype", "KARYOTYPE", "karyo", "Karyo",
"iscn", "ISCN".}

\item{id_column}{Character string specifying the column name to use as row
identifier in the output. If NULL (default), tries to auto-detect from common
names (sample_id, patient_id, id, mrn, subject_id, etc.) and prints a message
noting which column was selected (or that none was found). The ID column is
placed first in the output. Ignored when input is a character vector.}

\item{.return}{Character string specifying return type: "tibble" (default) or
"data.frame".}

\item{verbose}{Logical. If TRUE, prints validation reports and parsing messages.}

\item{on_issues}{Character string specifying how to handle any karyotype
issues detected by \code{check_karyo()}. Three classes of issues are handled:
\itemize{
\item \strong{Dirty markers} (e.g. leading dots, HTML entities, missing sex comma):
formatting artifacts that \code{preprocess_karyo()} can fix automatically.
\item \strong{Chimeric separators} (\verb{//}): karyotypes containing independent cell
populations. Under \code{"fix"}, only the portion before the first \verb{//} is
kept (the dominant clone). Information about secondary clones is lost.
\item \strong{Structural errors} (e.g. no chromosome count, \verb{Updated ISCN} marker):
unfixable — these rows always return NA regardless of \code{on_issues}.
}

Values:
\itemize{
\item \code{"fix"} (default): Apply \code{preprocess_karyo()} to dirty rows; truncate
chimeric rows to the first clone. Rows that still have issues after
these corrections are returned as NA. A message is printed summarising
how many rows were fixed and how many could not be fixed.
\item \code{"warn"}: Return NA for all issue rows and emit a message. No fixing
is attempted.
\item \code{"stop"}: Raise an error immediately if any issues are found. No fixing
is attempted.
}}
}
\value{
A tibble or data.frame with columns:
\itemize{
\item original_karyotype: Input karyotype string
\item ploidy_category: Classification (diploid, hyperdiploid, etc.) based on most abnormal clone
\item One column per aberration flag (0/1 binary)
\item monoX, monoY, mono1-22: Monosomy flags for each chromosome
\item trisX, trisY, tris1-22: Trisomy flags for each chromosome
\item normal_karyotype: 1 if 46,XX or 46,XY, else 0
\item total_metaphases: Count from bracket notation
\item comma_count_aberrations: Number of comma-separated aberrations
\item complex_karyotype: 1 if >=3 unique aberrations, else 0
\item monosomal_karyotype: 1 if meets monosomal criteria, else 0
\item mixed_ploidy: 1 if clones have different ploidy categories, else 0
\item issues: List-column; NULL for clean rows, tibble of issues for problem rows
}
}
\description{
Parses ISCN karyotype notation into structured binary features for analysis.
Extracts specific translocations, deletions, monosomies, trisomies, and other
chromosomal aberrations according to configurable rules.
}
\examples{
# Basic usage with character vector
karyotypes <- c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
result <- parse_karyo(karyotypes)

# Usage with data.frame
df <- data.frame(
  sample_id = c("S1", "S2", "S3"),
  iscn = c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
)
result <- parse_karyo(df, karyotype_column = "iscn")

# Return as data.frame instead of tibble
result <- parse_karyo(df, karyotype_column = "iscn", .return = "data.frame")

# Use verbose mode for debugging
result <- parse_karyo(df, karyotype_column = "iscn", verbose = TRUE)

}
