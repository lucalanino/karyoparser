% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_karyo.R
\name{parse_karyo}
\alias{parse_karyo}
\title{Parse ISCN Karyotype Strings}
\usage{
parse_karyo(
  karyotypes,
  rules = myeloid_rules,
  karyotype_column = NULL,
  id_column = NULL,
  verbose = FALSE,
  on_issues = c("stop", "preprocess", "warn"),
  on_chimeric = c("default", "host", "donor"),
  columns = NULL,
  min_metaphases = 2
)
}
\arguments{
\item{karyotypes}{A character vector of karyotype strings, a data frame
containing a karyotype column, or a \code{karyo_preprocessed} tibble returned
by \code{preprocess_karyo()}. When a \code{karyo_preprocessed} object is passed, the
\code{preprocessed} column is used automatically and cached issue indices and id
column are reused -- no additional arguments required.}

\item{rules}{A \code{karyo_rules} object (default: \link{myeloid_rules}), or a list
of \code{karyo_rules} objects to combine (e.g. \code{list(myeloid_rules, lymphoid_rules)}). Rules already fire independently within a single
table -- multiple rules can match the same token -- so combining tables
just widens the pool. \code{flag_name} must be unique across the combined
tables: \code{\link[=validate_rules]{validate_rules()}} errors if the same \code{flag_name} appears more
than once, since there is no cross-table priority to resolve the
conflict. Use \code{\link[=validate_rules]{validate_rules()}} to validate and convert a custom data
frame into an accepted rules object. Each rule's output column is a
sanitized version of its \code{flag_name} (non-alphanumeric characters
replaced by \verb{_}), e.g. \code{"t(9;22)(q34;q11)"} becomes \code{t_9_22_q34_q11}.}

\item{karyotype_column}{Character. Name of the karyotype column. \strong{Required}
when input is a plain data frame -- columns are never guessed, since a
frame carrying more than one plausible candidate (a raw \code{iscn} beside a
cleaned \code{karyotype}) would otherwise be parsed from the wrong one
silently. Ignored for character vector or \code{karyo_preprocessed} input.}

\item{id_column}{Character. Name of the id column. Optional: \code{NULL}
(default) means the data has no identifier, and one is never inferred.
When given, the column is placed first in the output and carried through
the pipeline.
Duplicate ids are warned about, not rejected: rows are matched on the
karyotype string and never on the id, so parsing is unaffected -- but
downstream joins on the column may fan out.

For \code{karyo_preprocessed} input the id is inherited automatically and
\code{id_column} is rarely needed. \code{preprocess_karyo()} returns a narrow
tibble -- its id column plus \code{original}, \code{preprocessed}, and \code{status} --
so every other column of the source data frame is already gone, and
naming one here is an error rather than an override. To key the output
on a different column, name it upstream (\code{check_karyo()} or
\code{preprocess_karyo(..., id_column = )}), or attach it to the preprocessed
tibble yourself before parsing. The three structural column names are
reserved and rejected as \code{id_column}.}

\item{verbose}{Logical. If \code{TRUE}, prints column detection and parsing
summary messages. Default \code{FALSE}.}

\item{on_issues}{Character string specifying how to handle \strong{dirty markers}
(formatting artifacts such as leading dots, HTML entities, missing sex
comma) and \strong{structural errors} (e.g. no chromosome count, \verb{Updated ISCN}
marker). Chimeric handling is independent of this argument -- see
\code{on_chimeric}. Structural errors are always unfixable and return NA
regardless of \code{on_issues}.

Values:
\itemize{
\item \code{"stop"} (default): Raise an error immediately if any non-chimeric issue
is found, listing fixable and unfixable issue types and counts, and
suggesting next steps. No fixing is attempted. Chimeric rows never
trigger the error -- they are clone-selected and parsed per
\code{on_chimeric}. \strong{Exception}: when input is a \code{karyo_preprocessed}
object, \code{"stop"} does not error -- unfixable rows are returned as NA and
a warning is emitted.
\item \code{"preprocess"}: Apply \code{preprocess_karyo()} to dirty rows. Rows that still
have issues after these corrections are returned as NA. A message is
printed summarising how many rows were fixed and how many could not be
fixed.
\item \code{"warn"}: Return NA for all dirty/structural issue rows and emit a
warning. No fixing is attempted.
}

Across all three modes, chimeric rows are clone-selected per \code{on_chimeric}
and parsed; a chimeric row becomes NA only when its selected clone is
itself unparseable (or it has two or more \verb{//} separators).}

\item{on_chimeric}{Character string controlling which clone is parsed for
chimeric karyotypes (those containing a \verb{//} separator). One of:
\itemize{
\item \code{"default"}: host clone (before \verb{//}) for normal chimeras; donor clone
(after \verb{//}) for zero-host chimeras (\verb{.//} prefix).
\item \code{"host"}: host clone only; zero-host chimeras return NA.
\item \code{"donor"}: everything after \verb{//} (the donor population).
Rows with two or more \verb{//} separators (\code{multiple_chimeric_separator}) are
always unfixable and return NA. For \code{karyo_preprocessed} input the clone
selection was baked in at preprocess time and is inherited; passing an
explicit, conflicting \code{on_chimeric} raises an error.
}}

\item{columns}{Character vector selecting which output-column classes to
include, or \code{NULL} (default) for the full output. \code{original_karyotype},
\code{preprocessed_karyotype}, \code{fixable_error}, \code{unfixable_error},
\code{chimeric_karyotype}, and \code{chimeric_clone} are always included regardless
of \code{columns}. Valid values (any combination):
\itemize{
\item \code{"classification"}: \code{normal_karyotype}, \code{complex_karyotype},
\code{monosomal_karyotype}
\item \code{"rule"}: rule-matching aberration flags (from \code{rules})
\item \code{"general"}: disease-agnostic structural flags (\verb{general_*},
\code{balanced_translocation}, \code{unbalanced_translocation})
\item \code{"aneuploidy"}: \verb{mono*}/\verb{tris*} monosomy/trisomy flags
\item \code{"summary"}: \code{comma_count_aberrations}, \code{chromosome_count},
\code{total_metaphases}
\item \code{"der_loss"}: \verb{unbal_partial_loss_<arm>} and \code{unbal_partial_loss}
}}

\item{min_metaphases}{Numeric. Minimum metaphase count for a clone to be
eligible to supply \code{chromosome_count}. Default \code{2}. Clones whose bracket
reports fewer metaphases are skipped, so a small side clone does not
outvote the main one; if no clone in a row clears the threshold,
\code{chromosome_count} is \code{NA} for that row. The threshold applies only to
rows where at least one clone carries a metaphase bracket -- a row with
no brackets anywhere uses all of its clones regardless. Raise it to
ignore more small clones (\code{5} is a common cytogenetics convention), or
set \code{0} to consider every clone that has a count.}
}
\value{
A tibble with the columns below, grouped by topic rather than
listed in output order:
\itemize{
\item original_karyotype: Input karyotype string (always the raw input)
\item preprocessed_karyotype: \code{normalize_iscn()} output of the
karyotype string, consistent across all \code{on_issues} modes. In
\code{"preprocess"} mode this is also dirty-fixed; in \code{"warn"}/\code{"stop"} modes
only \code{normalize_iscn()} is applied (chimeric rows are clone-selected in
every mode). \code{NA_character_} for issue rows.
\item chromosome_count: Integer chromosome count, taken from the most
abnormal eligible clone in the row (the one whose count is furthest
from 46). Eligibility is governed by \code{min_metaphases}; \code{NA} when no
clone in the row qualifies, which can happen even though
\code{total_metaphases} is non-\code{NA}.
\item One column per aberration flag (0/1 binary); rule-based columns are
named after a sanitized \code{flag_name} (see \code{rules} above)
\item monoX, monoY, mono1-22: Monosomy flags for each chromosome
\item trisX, trisY, tris1-22: Trisomy flags for each chromosome
\item normal_karyotype: 1 if 46,XX or 46,XY, else 0
\item total_metaphases: Count from bracket notation
\item comma_count_aberrations: Number of comma-separated aberrations
\item distinct_aberrations: Number of distinct normalized aberration
tokens pooled across all clones, excluding sex complements,
\code{idem}/\code{sl}, and range markers. This is the count
\code{complex_karyotype} thresholds at 3.
\item complex_karyotype: 1 if >=3 unique aberrations, else 0
\item monosomal_karyotype: 1 if the row has two or more autosomal
monosomies, or one autosomal monosomy plus at least one structural
aberration (any \verb{general_*} flag except \code{general_marker} and
\code{general_dmin} -- a lone marker or double minute does not count).
Two carve-outs: sex-chromosome monosomies (\code{monoX}/\code{monoY}) never
count toward either criterion, and a
CBF-AML row (\verb{t(8;21)(q22;q22)}, \code{inv(16)(p13q22)}, or
\verb{t(16;16)(p13;q22)}) is forced to 0 per clinical guidelines even
when the criteria are met.
\item balanced_translocation: 1 if the row carries a balanced translocation --
a bare \code{t(...)} token, or a reciprocal der pair where both partner
chromosomes appear as centromere donors (e.g.
\verb{der(5)t(5;17)...,der(17)t(5;17)...}). Independent of
\code{general_derivative}. A row may be both balanced and unbalanced.
\item unbalanced_translocation: 1 if the row carries an unbalanced
translocation -- a lone der whose reciprocal set is incomplete (e.g.
\verb{der(a)t(a;b)} or a lone \verb{der(a)t(a;b;c)} three-way der) or a whole-arm
\verb{der(a;b)}. The specific fusion flag still fires (e.g. \verb{der(9)t(9;22)}
keeps \code{t_9_22_q34_q11 = 1}). A der is balanced instead only when the
full reciprocal set is present (every partner appears as a der).
\item unbal_partial_loss_\if{html}{\out{<arm>}}: one column per chromosome arm
(\code{unbal_partial_loss_1p}, \code{unbal_partial_loss_1q}, ...,
\code{unbal_partial_loss_22q}, \code{unbal_partial_loss_Xp},
\code{unbal_partial_loss_Xq}, \code{unbal_partial_loss_Yp},
\code{unbal_partial_loss_Yq}), set to 1 when an unbalanced der
translocation implies partial loss of that arm. Kept SEPARATE from
\code{del(...)}/\verb{mono*} -- an unbalanced-derived 5q loss does not
set \code{del_5q}. Derivation needs explicit breakpoints and is limited to
simple single-junction two-partner \verb{der(a)t(a;b)}; multi-junction chains
and three-way \verb{t(a;b;c)} derivatives are flagged unbalanced but derive no
loss. Copy-number-aware gains are out of scope.
\item unbal_partial_loss: 1 if an unbalanced der translocation implies any
partial loss (OR across all \verb{unbal_partial_loss_*} columns).
\item fixable_error: 1 if the row had at least one fixable issue (dirty marker
or chimeric separator) and no unfixable issue, regardless of whether the
fixable issue was auto-corrected. 0 when \code{unfixable_error = 1}.
\item unfixable_error: 1 if row had unfixable structural issues
(row is NA), else 0
\item chimeric_karyotype: 1 if row contained a \verb{//} chimeric separator
(including zero-host \verb{.//} chimeras and \code{multiple_chimeric_separator}
rows), else 0
\item chimeric_clone: which clone was parsed for a chimeric row -- \code{"host"},
\code{"donor"}, or \code{NA_character_} (non-chimeric rows, or chimeric rows with
no selectable clone such as \code{multiple_chimeric_separator} or a zero-host
chimera under \code{on_chimeric = "host"})
}
}
\description{
Parses ISCN karyotype notation into structured binary features for analysis.
Extracts specific translocations, deletions, monosomies, trisomies, and other
chromosomal aberrations according to configurable rules. Performs no
cleaning or normalization itself -- that is \code{preprocess_karyo()}'s job --
but \code{on_issues = "preprocess"} invokes the same checking/fixing logic as
\code{check_karyo()}/\code{preprocess_karyo()} internally, so a single call is
usually enough without chaining all three explicitly.
}
\examples{
# Basic usage with character vector
karyotypes <- c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
result <- parse_karyo(karyotypes)

# Usage with data.frame
df <- data.frame(
  sample_id = c("S1", "S2", "S3"),
  iscn = c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
)
result <- parse_karyo(df, karyotype_column = "iscn")

# Use verbose mode for debugging
result <- parse_karyo(df, karyotype_column = "iscn", verbose = TRUE)

}
\seealso{
\code{\link[=check_karyo]{check_karyo()}}, \code{\link[=preprocess_karyo]{preprocess_karyo()}}, \code{vignette("data-cleaning")}
}
