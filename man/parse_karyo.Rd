% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/parse_karyo.R
\name{parse_karyo}
\alias{parse_karyo}
\title{Parse ISCN Karyotype Strings}
\usage{
parse_karyo(
  karyotypes,
  rules = myeloid_rules,
  karyotype_column = NULL,
  id_column = NULL,
  verbose = FALSE,
  on_issues = c("stop", "preprocess", "warn")
)
}
\arguments{
\item{karyotypes}{A character vector of karyotype strings, a data frame
containing a karyotype column, or a \code{karyo_preprocessed} tibble returned
by \code{preprocess_karyo()}. When a \code{karyo_preprocessed} object is passed, the
\code{preprocessed} column is used automatically and cached issue indices and id
column are reused -- no additional arguments required.}

\item{rules}{A \code{karyo_rules} object (default: \link{myeloid_rules}). Use
\code{\link[=validate_rules]{validate_rules()}} to validate and convert a custom data frame into an
accepted rules object.}

\item{karyotype_column}{Character. Name of the karyotype column when input is
a plain data frame. If \code{NULL} (default), auto-detected from common names
(\code{karyotype}, \code{iscn}, etc.). Ignored for character vector or
\code{karyo_preprocessed} input.}

\item{id_column}{Character. Name of the id column when input is a data frame.
If \code{NULL} (default), auto-detected from common names (\code{sample_id},
\code{patient_id}, \code{id}, \code{mrn}, etc.) and used silently. The id column is
placed first in the output. For \code{karyo_preprocessed} input, the id is
propagated automatically
from upstream pipeline steps; pass \code{id_column} explicitly only to override.}

\item{verbose}{Logical. If \code{TRUE}, prints column detection and parsing
summary messages. Default \code{FALSE}.}

\item{on_issues}{Character string specifying how to handle any karyotype
issues detected by \code{check_karyo()}. Three classes of issues are handled:
\itemize{
\item \strong{Dirty markers} (e.g. leading dots, HTML entities, missing sex comma):
formatting artifacts that \code{preprocess_karyo()} can fix automatically.
\item \strong{Chimeric separators} (\verb{//}): karyotypes containing independent cell
populations. Under \code{"preprocess"}, only the portion before the first \verb{//}
is kept (the dominant clone). Information about secondary clones is lost.
\item \strong{Structural errors} (e.g. no chromosome count, \verb{Updated ISCN} marker):
unfixable -- these rows always return NA regardless of \code{on_issues}.
}

Values:
\itemize{
\item \code{"stop"} (default): Raise an error immediately if any issues are found,
listing fixable and unfixable issue types and counts, and suggesting next
steps. No fixing is attempted. Use this to catch data quality problems
early; switch to \code{"preprocess"} or run \code{check_karyo()} once
you understand them. \strong{Exception}: when input is a
\code{karyo_preprocessed} object (i.e. the user has already run
\code{check_karyo()} and \code{preprocess_karyo()}), \code{"stop"} does not
error -- unfixable rows are returned as NA and a warning is emitted.
\item \code{"preprocess"}: Apply \code{preprocess_karyo()} to dirty rows;
truncate chimeric rows to the first clone. Rows that still have
issues after these corrections are returned as NA. A message is
printed summarising how many rows were fixed and how many could
not be fixed.
\item \code{"warn"}: Return NA for all issue rows and emit a warning. No fixing
is attempted.
}}
}
\value{
A tibble with columns:
\itemize{
\item original_karyotype: Input karyotype string (always the raw input)
\item preprocessed_karyotype: \code{normalize_iscn()} output of the
karyotype string, consistent across all \code{on_issues} modes. In
\code{"preprocess"} mode this is
also dirty-fixed and chimeric-truncated; in \code{"warn"}/\code{"stop"} modes only
\code{normalize_iscn()} is applied. \code{NA_character_} for issue rows.
\item ploidy_category: Classification (diploid, hyperdiploid, etc.)
based on most abnormal clone
\item chromosome_count: Integer chromosome count extracted from the karyotype
\item One column per aberration flag (0/1 binary)
\item monoX, monoY, mono1-22: Monosomy flags for each chromosome
\item trisX, trisY, tris1-22: Trisomy flags for each chromosome
\item normal_karyotype: 1 if 46,XX or 46,XY, else 0
\item total_metaphases: Count from bracket notation
\item comma_count_aberrations: Number of comma-separated aberrations
\item complex_karyotype: 1 if >=3 unique aberrations, else 0
\item monosomal_karyotype: 1 if meets monosomal criteria, else 0
\item mixed_ploidy: 1 if clones have different ploidy categories, else 0
\item fixable_error: 1 if the row had at least one fixable issue (dirty marker
or chimeric separator) and no unfixable issue, regardless of whether the
fixable issue was auto-corrected. 0 when \code{unfixable_error = 1}.
\item unfixable_error: 1 if row had unfixable structural issues
(row is NA), else 0
\item chimeric_karyotype: 1 if row contained a \verb{//} chimeric separator (regular
chimeric rows are truncated to the host clone; \code{zero_host_chimera} rows
are also flagged here and returned as NA via \code{unfixable_error})
}
}
\description{
Parses ISCN karyotype notation into structured binary features for analysis.
Extracts specific translocations, deletions, monosomies, trisomies, and other
chromosomal aberrations according to configurable rules.
}
\examples{
# Basic usage with character vector
karyotypes <- c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
result <- parse_karyo(karyotypes)

# Usage with data.frame
df <- data.frame(
  sample_id = c("S1", "S2", "S3"),
  iscn = c("46,XX", "47,XY,+21", "46,XX,t(15;17)(q24;q21)")
)
result <- parse_karyo(df, karyotype_column = "iscn")

# Use verbose mode for debugging
result <- parse_karyo(df, karyotype_column = "iscn", verbose = TRUE)

}
